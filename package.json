{
  "name": "gitpod",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.4",
    "nodemailer": "^7.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
